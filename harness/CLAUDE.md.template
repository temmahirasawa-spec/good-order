# CLAUDE.md — {{PROJECT_NAME}}

このファイルはこのリポジトリの**規約**です。作業を始める前に必ず読み、以下に従ってください。
流動的な実装の経緯・判断の履歴は `docs/handoff.md` にあります。実装に入る前にそちらも読んでください。

<!--
  ══════════════════════════════════════════════════════════════
  この雛形の使い方（移行が終わったらこのコメントごと消すこと）

  1. `{{ }}` を全部埋める。埋めるべき一覧は harness/README.md の
     「プレースホルダ一覧」を見る
  2. 使わない節は**丸ごと消す**。空のまま残すと、AI が「無い決まりを
     守ろうとする」か「あるはずのファイルを探し続ける」ので害になる
  3. 章の順番は変えない。3章（止まる／進む）と 2章（完了の定義）が
     この規約の芯なので、ここを薄くすると全体が効かなくなる

  この雛形の設計思想:
    ・「お願い」は忘れられる。**判定できるものは機械の制約に上げる**
      （会話 → このファイル → npm run check の3段階。8章の表を参照）
    ・AI を止めるのは安全ではなく、単に作業が進まないだけ。
      **止まる項目を絞って明示する**（3章）
    ・非エンジニアが読んで判断できる形で報告させる（1章）
  ══════════════════════════════════════════════════════════════
-->

---

## 1. このリポジトリについて

**{{PROJECT_NAME}}** — {{PROJECT_TAGLINE}}。{{OWNER_ORG}} の{{PROJECT_KIND}}。
{{FIRST_MILESTONE}}

- {{AUDIENCE_A_LABEL}}: {{AUDIENCE_A_ROUTES}}
- {{AUDIENCE_B_LABEL}}: {{AUDIENCE_B_ROUTES}}

スタック: {{STACK}}

### {{USER_NAME}}とのやりとり

- **常に{{LANGUAGE}}で応答する。** それ以外の言語で書かない
- **{{USER_NAME}}はエンジニアではない。** 専門用語を使うときは、必ず直後に「＝何が起きるか」を平易な言葉で添える
  - 例：「ブランチを切ります（＝正式版のコピーを作ります。本番には影響しません）」
- **確認を求めるときは、以下を必ず書く**
  1. 何をしようとしているか（{{LANGUAGE}}で、1文）
  2. それをすると何が起きるか
  3. やらなかった場合どうなるか
  4. 取り消せるかどうか
- **「〜してもいいですか？」だけの質問をしない。** 判断材料が無いまま承認を求めない
- **選択肢がある場合は、番号をつけて、それぞれ何が違うかを1行で書く**

---

## 2. 完了の定義

**`npm run check` が通っていない作業は、完了ではありません。**

- 「実装しました」「修正しました」と報告する前に、**必ず自分で `npm run check` を実行する**
- 落ちたら、報告せずに**自分で直してから**再実行する。通るまで繰り返す
- 人間に「エラーが出ました、どうしますか」と投げ返さない。それは作業を完了させていないのと同じ
- 3回直しても同じ箇所で落ちる場合だけ、**何を試して何が起きたか**を添えて相談する

`npm run check` = `typecheck`（`tsc --noEmit`）→ `lint`（`next lint --max-warnings 0`）
→ `design`（デザイントークンQA）→ `build`（`next build`）。1つでも落ちたらそこで止まる。

UIを変更した場合は、`{{UI_GALLERY_PATH}}`（コンポーネントギャラリー）に該当コンポーネントの
セクションを追加・更新すること。目視確認の導線を切らさないため。

画面に見える変更を加えた場合は、`npm run check` に加えて、Playwright MCP で該当ページを開いて
スクリーンショットを撮り、それを提示するまでを完了とする。撮るのは **PC幅 {{PC_WIDTH}}px** と
**スマホ幅 {{SP_WIDTH}}px** の2枚。dev サーバーが起動していない場合は、**起動を求めて止まる**こと
（勝手にバックグラウンドで起動しない）。

---

## 3. 止まって確認すること / 自分で判断してよいこと

### 必ず止まって{{USER_NAME}}に確認する

<!-- ここは「間違えると金・信用・データが失われる」ものだけを書く。
     多すぎると全部が形骸化するので、5〜6項目に絞ること。 -->

- **デザインの意思決定** — Figma に対応するノード・変数が無く、レイアウト／配色／タイポを自分で決める必要があるとき
- **{{CRITICAL_DOMAIN}}のロジック変更** — {{CRITICAL_DOMAIN_EXAMPLES}}
- **クライアント・お客様の目に触れる文言** の新規作成や変更
- **DBのスキーマ変更・破壊的マイグレーション** — 列の削除、型変更、RLSポリシーの緩和
- **外部公開される内容** の変更 — OGP、manifest、sitemap、robots、公開URL

### 確認せずに自分で判断して進めてよい

以下でいちいち止まらないでください。止まることは安全ではなく、単に作業が進まないだけです。

- **Figma に定義済みのデザインの実装** — 対応するノードや変数がある実装は「判断」ではなく「写し取り」。自走してよい
- `{{DESIGN_TOKENS_PATH}}` に定義済みのトークンの適用
- 型エラー・lintエラー・ビルドエラーの修正
- リファクタリング、命名、ファイル分割（公開挙動が変わらない範囲）
- `docs/handoff.md` の更新
- コミットとpush（下記 8 を参照）

判断に迷ったら、**まず `docs/handoff.md` の過去の判断履歴を探す**こと。同じ論点が既に決着している場合が多い。

---

## 4. 技術規約

実装を始める前に、`docs/specs/` 配下に該当する仕様ファイルがないか確認し、あれば必ず読むこと。
仕様に書かれていない判断が必要になった場合、および仕様と異なる実装が必要になった場合は、
勝手に進めず{{USER_NAME}}に確認すること。

Figma でデザイン作業を行う場合は、着手前に必ず `docs/specs/design-rules.md` を読むこと。
同ファイルの「4. 完了の定義」を満たすまで作業完了としないこと。特に、ピクセルを作る前に
操作の流れを文章で合意すること、案は必ず3つ出すこと、PC を作るときは SP も対で作ること、
作業の最後に整理整頓と `npm run design:figma` を通すことは必須。

### インフラ

- Vercel の関数リージョンは **{{VERCEL_REGION_LABEL}} `{{VERCEL_REGION}}`** 固定（`vercel.json`）。{{VERCEL_REGION_REASON}}。変更しない
- `.env.local` は**絶対にコミットしない**。**このリポジトリは {{REPO_VISIBILITY}} です**

### UI の実装

- **HTMLアウトプットは{{COLOR_SCHEME}}固定。{{COLOR_SCHEME_EXCLUDED}}は実装しない**
- トグル・チェック等の即時フィードバックが要る操作は**楽観的更新**で実装する。
  ローカル状態を先に更新 → 永続化 → **失敗したときだけロールバック**
- 同期的な `await action()` → `router.refresh()` パターンは**使わない**。
  画面全体の再フェッチは体感が重く、操作の手触りが死ぬ

### デザイントークン

<!-- Figma を使わないプロジェクトでは、この節と package.json の design / design:figma、
     scripts/ の2ファイルを丸ごと落とすこと（README の Step 3 参照）。 -->

- `{{DESIGN_TOKENS_PATH}}` が実装側の参照先。**source of truth は Figma Variables**（file key `{{FIGMA_FILE_KEY}}`）。
  Figma で値が変わったらこのファイルを同期する
- {{TOKEN_USAGE_SCOPE}}では `p-[var(--space-16)]` `rounded-[var(--radius-xl)]` のような
  任意値記法、または `style={{ }}` で CSS変数を直接参照する。
  これらは Tailwind のデフォルトと同名で値が違うため、**`tailwind.config.ts` には意図的にマージしていない**
- **生の色コードを直接書かない。** {{BRAND_SWITCH_NOTE}}
- これは **`npm run design` が機械的に検出する**（`scripts/check-design-tokens.mjs`）。
  - トークンに無い色 → デザイン判断が必要。**勝手に決めず{{USER_NAME}}に確認する**（上記 3 参照）
  - トークンと同値の直書き → `var(--...)` に置き換える
  - SVG は属性に `var()` を書いても解決されない。
    `<svg style={{ color: "var(--...)" }}>` ＋ `stroke="currentColor"` の形にする
  - PWA の meta や QR 生成など、**構造的に CSS変数が使えない**箇所だけ、
    その行か直前のコメントに `design-qa-allow: 理由` と書いて除外する。理由は必ず書く

### {{DATABASE_NAME}}

- SQL は `{{SQL_DIR}}` 配下に**1機能1ファイル**で追加する。既存ファイルを書き換えない
- **RLSを緩めない**。特に{{SENSITIVE_PERMISSION_NOTE}}
- 既存のビジネスロジック・データフローは、指示されない限り**変更しない**。
  リデザイン作業では「見た目だけを差し替える」が原則

### 互換のため意図的に残している名前

<!-- 改名・リブランドを経ていないプロジェクトでは、この節ごと消す。
     残す場合は「なぜ変えられないのか」を必ず書く。理由の無い禁止事項は破られる。 -->

{{LEGACY_NAMES_REASON}}

{{LEGACY_NAMES_LIST}}

---

## 5. ブランド・表記規約

<!-- 表記ゆれが起きるものだけ書く。守れているものは書かない。 -->

- プロダクト名称は **{{BRAND_CANONICAL_NAME}}** で統一。{{BRAND_DEPRECATED_NAMES}}
- ブランド名の正式スペルは **{{BRAND_SPELLINGS}}**
- {{NAME_ORDER_RULE}}

---

## 6. ディレクトリの読み方

<!-- 「そのディレクトリを開かなくても役割が分かる」ことがこの表の目的。
     ファイル一覧ではないので、機械的に全部書かない。迷いやすいものだけ書く。 -->

| パス | 役割 |
|---|---|
| `app/` | App Router。`(protected)` は認証ガード付き。ルートごとに `layout.tsx` でメタデータを定義 |
| `{{UI_GALLERY_PATH_FS}}` | コンポーネントギャラリー。Figma との目視突き合わせ用。**本番導線からリンクなし・認証なし** |
| `components/` | 共通コンポーネント |
| `lib/` | ロジック・{{DATABASE_NAME}}クライアント・状態管理 |
| `hooks/` | 汎用React hooks |
| `{{SQL_DIR}}` | SQLマイグレーション。1機能1ファイル |
| `.github/workflows/` | GitHub Actions。`check.yml` が PR と {{DEFAULT_BRANCH}} で `npm run check` を回す |
| `scripts/` | 検査スクリプト。`check-design-tokens.mjs` が生の色コードを検出する |
| `docs/specs/` | 仕様。実装前に該当ファイルを読む |
| `docs/handoff.md` | 実装の経緯と判断の履歴。**セッション開始時に読む** |
| `{{ASSETS_DIR}}` | 素材ストック。**Next.js の配信対象外**（`public/` と混同しない） |

---

## 7. やってはいけないこと

<!-- 「実際に一度やらかしたこと」だけを書く。予防的に書き足すと形骸化する。 -->

- `.env.local` をコミットする（**{{REPO_VISIBILITY}} リポジトリ**）
- `node_modules/` `.next/` `tsconfig.tsbuildinfo` を手で編集する
- `npm run check` を通さずに完了を宣言する
- {{PROJECT_SPECIFIC_DONT_1}}
- {{PROJECT_SPECIFIC_DONT_2}}

---

## 8. ブランチとコミットの規約

### ブランチ

- **`{{DEFAULT_BRANCH}}` への直接 push は禁止**。GitHub のブランチ保護で機構的に不可能（管理者にも適用済み）
- 作業は必ず feature ブランチを切り、**PR 経由で `{{DEFAULT_BRANCH}}` に入れる**
- ブランチ名は `種別/短い説明`。種別は `feat` / `fix` / `refactor` / `docs` / `chore` / `ci`
  例: `feat/order-optimistic-cart`

### コミット

- **Conventional Commits ＋ {{LANGUAGE}}**。例: `feat(order): カート投入時の楽観的更新を追加`
- **各コミットは、それ単体で `npm run check` が通る状態にする**。
  意味の単位ではなく「通る単位」で割る

### PR とマージ

作業が完了したら、AI は次のところまで**自分で実行する**。手順は以下。

1. `git checkout -b 種別/説明`
2. 実装 → `npm run check` が通るまで自分で直す
3. コミット（説明文は{{LANGUAGE}}。Conventional Commits）
4. `git push -u origin <ブランチ名>`
5. `gh pr create` で PR を作成する。**`--fill` は使わない。** 本文には必ず以下の3項目を書く。
   - 変更内容（箇条書き、1行ずつ）
   - プレビューで見てほしいページのパス
   - Playwright で撮った PC {{PC_WIDTH}}px / SP {{SP_WIDTH}}px のスクリーンショット
6. **ここで止まる。AI は `gh pr merge` を実行しない。**

- マージの判断は{{USER_NAME}}が行う。{{USER_NAME}}がプレビューを見て問題なければ、{{USER_NAME}}自身がマージする
- **マージは squash のみ。** `{{DEFAULT_BRANCH}}` の履歴は一直線に保つ
- **CI（GitHub Actions の `check`）が落ちたら、報告せずに自分で直す。**
  ローカルの Stop hook とまったく同じ扱い（上記 2 を参照）

### 例外

- 上記 3 の「止まって確認する」項目を含む変更は、**PR を作るところまでで止めて**
  {{USER_NAME}}に確認を取る（通常フローと同じく、AI はここでも `gh pr merge` を実行しない）
- **`gh pr merge`、`gh pr merge --admin`、およびブランチ保護の一時解除は、AI は絶対に使わない。**
  マージは{{USER_NAME}}だけが行う

<!--
  この最後の2行は、`.claude/settings.json` の permissions.deny でも同じことを
  禁止している。**文章と設定の両方に書くこと。** 文章だけだと解釈で緩むし、
  設定だけだと「なぜ禁止なのか」が伝わらず、迂回路を探されることがある。
-->
