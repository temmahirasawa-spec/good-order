# Step3-L / Step3-M の実装をFigma実物と突き合わせて修正

## 前提：Figmaファイルの見え方について（重要）

handoff.mdに「画面テンプレートはこのファイルに存在しない（Componentsページにコンポーネント
しか無い）」と記録されていますが、**これは誤りです。**

このFigmaファイル（`KGPuY4YVRQW6BMRrulBaFN`）には5ページあります:

| ページ名 | node ID |
|---|---|
| Components | `46:16` |
| **MobileOrder** | **`32:2`** ← 画面テンプレートは全部ここ |
| ロゴ | `0:1` |
| コーポレートサイト | `18:2` |
| 参考サイト | `60:762` |

`get_metadata`を引数なしで呼ぶとComponentsページしか返らないようですが、`nodeId: "32:2"`を
渡せばMobileOrderページの中身が取れるはずです。取れない場合は、以下に実測値を全部書いて
あるので、そちらを正として進めてください。

Step3-L・Mはテンプレートを見ずに推定で実装された箇所があるため、その突き合わせが今回の作業です。

---

## 1. カテゴリ管理PC（`Template / Categories Management 1180x820`, node `309:279`）

### 1-1. 表示順バッジ（推定実装されていた箇所・要修正）

現在の実装は`bg-bg-tertiary`のピル＋`type-jp-label`で「**表示順 N**」と表示していますが、
Figma実物は以下です。

- サイズ: **21 × 22**（ほぼ正円）
- 角丸: `radius/full`（999）
- 背景: `bg/tertiary`
- パディング: 左右8 / 上下3
- 中身: **数字のみ**（`1`）。**「表示順」というラベル文字は入っていません**
- テキストスタイル: **`EN/Data/S`**

→ ラベル文字を削除し、数字のみの円形バッジに修正してください。

### 1-2. 一覧行のその他の実測値（差分があれば合わせてください）

行全体: 幅912 / 高さ56 / 上下パディング8 / 要素間 12px、横並びで以下の順:

1. ⠿ グリップアイコン（16×16）
2. サムネイル画像（40×40、角丸8）
3. **タグ色ドット（16×16の正円）**
4. テキスト列（要素間2px）
   - カテゴリ名: **`JP/Body Bold`**
   - スラッグ: **`JP/Label`**
5. 表示順バッジ（上記1-1）

`List Scroll`の左右パディングは24、行間は0（行自体の上下パディング8で間隔を取っています）。

### 1-3. Top Bar

- 高さ78 / 左右パディング24 / 上20 / 下16
- タイトル「カテゴリ管理」
- 右側ボタン: **135×42、角丸999、背景`accent/primary`、文言は「＋ カテゴリ追加」**
  （「＋ 新規追加」ではありません）

### 1-4. ヒント文言

Figma側を修正しました。**PC・SPとも以下が正**です。

```
⠿ をドラッグして並び替えると、メニュー画面での表示順が変わります
```

（旧「注文画面での表示順が…」から変更。メニュー管理側のPCテンプレートも同様に修正済みなので、
そちらも実装が古い文言のままなら合わせてください）

---

## 2. Color Swatch Picker（node `306:1535`）— 確認結果：修正不要

handoff.mdで「Figmaの説明文には『黒い外枠+チェック』とあるが実描画は外枠のみ、要確認」と
なっていた件を確認しました。

**実物は「選択中のスウォッチに2pxの`text/primary`（ink）の外枠が付くのみ」で、チェックマークは
入っていません。** 現在の実装（外枠のみ）が正しいので、このままで問題ありません。

スウォッチ自体は32×32、10色です。

---

## 3. テイクアウト受渡（`Template / Takeout Pickup 1180x820`, node `462:2942` /
`Takeout Pickup — Mobile 390`, node `462:3083`）

こちらもテンプレートを見ずに実装されているため、突き合わせをお願いします。

### Pickup Card（node `462:2923`）の実測値

- カード: 幅400（PC）/ 角丸12 / 背景`surface/white` / 影は薄め（y1・blur6・6%）
- **Header**: 左右パディング20・上16・下8、左右振り分け
  - 左: 🛍 Bagアイコン（16×16）+ 受渡番号（**`JP/Heading/S`**、要素間6px）
  - 右: 経過時間（**`JP/Caption`**、`text/secondary`）
- **Items**: 左右パディング20
  - 各行: 上下パディング10、左右振り分け
  - 商品名: **`JP/Body`**（`text/primary`）
  - 数量: **`EN/Data/XS`**（`text/secondary`）、`× N`の形式
  - 行間に`border/divider`の1px区切り線（**最終行の下には入れない**）
- **Footer**: 左右パディング20・上12・下16、**右寄せ**
  - ボタン: 角丸999 / 背景`surface/ink` / 左右パディング20・上下10 /
    ラベルは`JP/Heading/S`の`text/inverse`

### PCのグリッド
`Main`側は左右パディング32・下32、カード間24px（縦横とも）のラップグリッドです。
現在`lg:grid-cols-2`とのことなので、間隔だけ確認してもらえれば大丈夫です。

### 文言について（差分は許容済み）
handoff.mdに記録の通り、ナビ・Top Bar・ボタンの文言を「テイクアウト受渡」「受渡完了」に
短縮した件は、幅の都合による意図的な差分として**そのままで構いません**。Figma側の文言に
合わせ直す必要はありません。

---

## 4. 報告してほしいこと

- 上記のうち、実際に差分があって修正した箇所
- Figmaの`nodeId: "32:2"`でMobileOrderページの中身が取れたかどうか
  （取れるなら今後のプロンプトで参照が楽になるので知りたいです）
