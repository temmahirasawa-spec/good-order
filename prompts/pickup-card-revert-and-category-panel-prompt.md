# Pickup Cardヘッダーの差し戻し ＋ カテゴリ編集パネルの確定

Figma側は全て修正済みです。以下に合わせてください。

---

## 1. Pickup Cardヘッダーを「番号を大きく」に戻す（差し戻し）

先の突き合わせで、ヘッダーを1行の`JP/Heading/S`に戻してもらいましたが、**これは戻しすぎでした。**

「受渡番号をどの画面でも最も大きく表示する」というのは天真さんの明示的な決定で、
22px化はそれに従った正しい実装でした。Figmaの方が古かったのが原因です
（あのカードは受渡番号の仕様が決まる前にデザインしたものでした）。

**Figma側を「ラベル＋大きい番号」に更新済み**なので、実装もそちらに合わせてください。

### 修正後のヘッダー構成（Figma実測）

```
受渡番号            8分経過
🛍 #01
```

- **左側は縦2段**（要素間2px）
  - 上段: ラベル「**受渡番号**」— `JP/Chip Label`（13px Bold）／`text/secondary`
  - 下段: 🛍 Bagアイコン（16×16）+ 番号「**#01**」— 横並び・要素間6px
    - 番号のテキストスタイルは **`EN/Data/L`（Barlow SemiBold 20px）**／`text/primary`
    - 前回の実装は生の22pxでしたが、既存トークンに揃えるため**20pxの`EN/Data/L`**を使ってください
      （カード内の他要素は15px・12pxなので、20pxでも十分「最も大きい要素」になります）
- **右側**: 経過時間（`JP/Caption`／`text/secondary`）。左カラムが2段になったので、
  **上端揃え**（`align-items: flex-start`）にしてください
- ヘッダーのパディング: 左右20・上16・下10

同じ扱いを`/complete`・`/history`・レジ画面にも適用済みだと思いますが、
そちらは今回変更していないはずなので、現状のままでOKです。

---

## 2. カテゴリ編集パネルの3差分 — 結論

報告してもらった3点、いずれも**実装が正しかった**ので、Figma側を実装に合わせて修正しました。
**コードの変更は不要**です（確認だけお願いします）。

| 項目 | 結論 | 理由 |
|---|---|---|
| ラベル | **実装のまま**（「スラッグ（URL）」「キャッチコピー」） | 括弧内の英語は開発者向けのフィールド名。店舗スタッフに「slug」は伝わらない |
| カテゴリ画像 | **実装のまま**（幅いっぱいの横長） | この画像はメニュー画面のカテゴリカード（約2.1:1）の背景になる。4:3のプレビューだと上下が切れることが分からず、縦長写真を選ぶ事故が起きる |
| フッターボタン | **実装のまま**（48px＋15px） | SP版が48px＋`JP/Heading/S`なので、PCのFigmaだけが46px/14pxで浮いていた |

Figma側はPC（`309:1748`）・SP（`429:2685`）とも上記に合わせて更新済みです。

---

## 3. 報告してほしいこと

- Pickup Cardヘッダーの差し戻し後の見た目（`/dev/ui`で確認できれば十分です）
- カテゴリ編集パネル側で、上記3点について実装を変える必要が無いことの確認
