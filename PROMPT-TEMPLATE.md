# ループ起動プロンプト テンプレート

AIに作業を投げるときの雛形。下の「テンプレ本体」をコピーして埋める。

**このテンプレの前提**：`npm run check` と Stop hook が「壊れていないか」を判定してくれる。
だからこのプロンプトに書くべきなのは**手順ではなく、何ができていたら完成かの定義**。

---

## テンプレ本体（ここからコピー）

```
## ゴール
（何が達成されたら終わりか。1〜2文で）

## 完了条件
- npm run check が通ること
- （機械が判定できる条件を、判定できる言葉で書く）
- （目視が必要なものは /dev/ui に追加して、僕が見る前提であることを明記）

## 触ってよい範囲
（例：app/order/ 配下と components/ui/ のみ。lib/ のロジックは変更しない）

## 参照するもの
- Figma: （ノードID）
- 既存の類似実装: （ファイルパス）
- 判断に迷ったら docs/handoff.md の過去の判断履歴を探すこと

## 迷ったときのデフォルト
（止まるほどではないが判断が要る場面で、どちらに倒してほしいか）
```

---

## 埋めるときのコツ

### 完了条件は「機械が判定できる言葉」に翻訳する

ここが一番の勘所。書けた分だけ、AIが自走できる距離が伸びる。

| ダメな書き方 | 判定できる書き方 |
|---|---|
| いい感じにして | `npm run check` が通る |
| ちゃんと動くように | カート追加が楽観的更新で、通信失敗時にロールバックする |
| Figma通りに | Figma ノード `429:2534` と余白・角丸・色が一致し、`/dev/ui` に該当セクションがある |
| バグを直して | `lib/menu.ts` の型エラーを解消し、既存の呼び出し側を壊さない |

**翻訳できなかった部分が、そのまま自分の持ち場として残る。**
「良いか」は機械には判定できない。装飾美と機能美の釣り合い、タップの手触り、
実際に店舗で使えるか。そこは②で自分が見る。

### 「触ってよい範囲」は毎回書く

AIの自走距離が伸びるほど、暴走したときの被害範囲も広がる。
制約は権限設定だけでなく、プロンプトの側からも掛けられる。

### 手順は書かない

「まず〜して、次に〜して」は書かなくていい。それはAIが決める領域。
書きたくなったら、それは完了条件が曖昧なサイン。手順ではなく条件を足す。

---

## 記入例

```
## ゴール
/admin/dashboard のカードを Figma の新レイアウトに差し替える。

## 完了条件
- npm run check が通ること
- 既存のデータ取得ロジック（lib/salesData.ts）を一切変更していないこと
- PC（1400px）とSP（390px）の両方で崩れないこと
- /dev/ui にカードのセクションを追加し、僕が目視で確認できる状態にすること

## 触ってよい範囲
app/admin/(protected)/dashboard/ と components/dashboard/ のみ。
lib/ と supabase/ は変更しない。

## 参照するもの
- Figma: ノード 512:1204（Dashboard Card v2）
- 既存の類似実装: components/admin/menu/MenuPreviewCard.tsx
- 判断に迷ったら docs/handoff.md の過去の判断履歴を探すこと

## 迷ったときのデフォルト
Figma に無い状態（ローディング・空状態・エラー）が必要になったら、
既存の components/Skeleton.tsx の作法に合わせる。新規デザインは作らない。
```

---

## ルールのはしご — 同じことを2回言ったら1段上げる

同じ注意を繰り返している自分に気づいたら、それは検品係に戻っているサイン。
ルールは置き場所によって強さが変わる。

| 段 | 置き場所 | 強さ |
|---|---|---|
| 1 | その場のプロンプト | 1回きり。忘れられる |
| 2 | このテンプレ／`prompts/` | 再利用できる |
| 3 | **CLAUDE.md** | 毎回読まれる（＝お願いの最上位） |
| 4 | **hook / CI / lint ルール / 型** | **破れない（＝制約）** |

3段目までは「お願い」。4段目だけが「制約」。
守られないルールは、上げる段を間違えている。

---

## 何をハーネスに足すかの見つけ方

先回りして作り込まない。**実際に一度痛い目に遭ったものだけ**を機械化する。

日々の作業で、この2つだけメモしておく。

1. **自分が手で確認した瞬間** — ブラウザで見た、ターミナルで確認した、Supabaseを覗いた。
   その一つひとつが「まだ機械に渡せていない検品作業」＝次にハーネス化する候補。
2. **hook が邪魔になった瞬間** — 待たされてイライラした、直さなくていいものを直された。
   放置すると hook を切りたくなる。切ったら元の木阿弥なので、邪魔になったら緩める。

---

> ハーネスを増やす目的は、判定できるものを全部機械に押し付けて、
> **判定できないものだけが自分の手元に残る状態**を作ること。
